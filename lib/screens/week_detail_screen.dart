import 'package:flutter/material.dart';
class WeekDetailScreen extends StatelessWidget {
  const WeekDetailScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Week Detail')));
}
